"""Utility scripts package for test imports.

This file makes the `scripts` directory importable in tests, e.g.:

    from scripts import evals_to_cp

The modules are still runnable as CLIs via `python -m scripts.module`.
"""
